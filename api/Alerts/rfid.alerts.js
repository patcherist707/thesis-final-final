import { realtime, firebaseAdmin, firestore } from "../firebaseConfig.js";

export const rfidThresholdAlert = async(io) => {
  // try {
  //   const capacityCollection = firestore.collection('Capacity');
  //   const snapshot = await capacityCollection.get();

    
    
  //   snapshot.forEach(doc => {
  //     console.log(doc.id, '=>', doc.data());
  //   });
    
  // } catch (error) {
    
  // }


}